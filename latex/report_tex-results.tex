\section{Results}

With JupyterBook it is possible to make your narrative and data-analysis available in a single file, through Jupyter Notebooks. It is even possible to create interactive materials using widgets on the website, but leave them out in the PDF version (using tags). To run the code below, click the \includegraphics[width=0.7\linewidth]{files/on-d2871461b6411d6f9a82e222928e25ff.png}

 icon in the top right corner. This will connect with a kernel. If the kernel is loaded, click \includegraphics[width=0.7\linewidth]{files/play-f4fafd5d21ac51e9b6a705c2b34ce715.png}

 to run the code.

Figure~\ref{chi2_fit} shows the results of fitting our linear model to the data. Parameter $a$ is optimal when the chi-squared curve is at its minimum, which corresponds to the smallest residuals and the best match between model and data.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.7\linewidth]{files/chi2_fit-93f7a12e0d951979b48e1a946facc8c9.png}
\caption[]{Fitting our data to a linear model with a single parameter $a$. The optimal value of $a$ is found when the chi-squared curve (bottom) is at its minimum. The residuals (top right) are smallest at this point, and the model (top left) best matches the data.}
\label{chi2_fit}
\end{figure}